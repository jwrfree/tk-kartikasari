export type StatsItem = {
  value: string;
  label: string;
};

export type HighlightItem = {
  icon: string;
  title: string;
  description: string;
};

export type ProgramItem = {
  name: string;
  age: string;
  description: string;
  points: string[];
};

export type JourneyItem = {
  time: string;
  title: string;
  description: string;
  icon: string;
};

export type FAQItem = {
  question: string;
  answer: string;
};
